# WebDriver Configuration
# Set driver.mode to 'local' for local execution or 'remote' for Docker/Selenium Grid
driver.mode=remote

# For remote driver (Docker/Selenium Grid)
docker.host=http://localhost:4444
docker.browser=firefox
docker.platform=WINDOWS

# For local driver
browser=chrome
headless=false
